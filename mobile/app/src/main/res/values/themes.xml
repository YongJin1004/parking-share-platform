<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose uses its own theming, this is just for the Activity container -->
    <style name="Theme.ParkingShare" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
